export let feedBtn = document.querySelectorAll(".user-feed");

// feedBtn.forEach((elem) => {
//   elem.addEventListener("click", (e) => {
//     elem.style.borderBottom = '1px solid black'
//     // console.log(elem);
//   });
// });

// (method) ParentNode.querySelectorAll<K extends keyof HTMLElementTagNameMap>(selectors: K): NodeListOf<HTMLElementTagNameMap[K]> (+4 overloads)
